# ── Supabase (auth + accounts + quest/ledger DB) ────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ── Leonardo intelligence layer (reused, read-mostly) ───────────────────────
LEONARDO_NEO4J_URI=bolt://localhost:7687        # imagination graph (evidence)
LEONARDO_NEO4J_USER=neo4j
LEONARDO_NEO4J_PASSWORD=leonardo_sf_ideas
CLAW_MEMORY_NEO4J_URI=bolt://localhost:7688     # council memory
CLAW_MEMORY_NEO4J_USER=neo4j
CLAW_MEMORY_NEO4J_PASSWORD=

# ── Base / onchain (TESTNET ONLY — never mainnet, never real $LEO) ──────────
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
BASE_MAINNET_RPC_URL=https://mainnet.base.org   # reads only (e.g. read live ERC-8004)
ERC8004_IDENTITY_REGISTRY=                       # deployed registry address (fill in M1)
ERC8004_REPUTATION_REGISTRY=
ERC8004_VALIDATION_REGISTRY=
PLATFORM_TESTNET_PRIVATE_KEY=                     # funded Base Sepolia key for write spikes

# ── x402 (per-call metering) ────────────────────────────────────────────────
X402_FACILITATOR_URL=                            # Coinbase testnet facilitator
X402_PAY_TO_ADDRESS=

# ── Council service (single-tenant platform credential — see roadmap) ───────
COUNCIL_CLAUDE_CREDENTIALS=                       # path to ~/.claude/.credentials.json
OPENCLAW_AGENTS_DIR=                              # ~/.openclaw/agents (seat persona files)

# ── Hosted agents (per-tenant metered model billing — NOT a subscription) ───
AGENT_MODEL_PROVIDER=openrouter
AGENT_MODEL_API_KEY=
